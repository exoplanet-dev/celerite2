version: 2

submodules:
  include: all

build:
  tools:
    python: mambaforge-4.10

python:
  # version: 3.8
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

conda:
  environment: docs/environment.yml

sphinx:
  builder: dirhtml
