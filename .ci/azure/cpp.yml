trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    mkdir -p build
    cd build
    cmake -DCMAKE_BUILD_TYPE=Debug ..
    make
  displayName: 'Build'

- script: |
    cd build
    CTEST_OUTPUT_ON_FAILURE=1 make test
  displayName: 'Test'
